# Complete Document Generation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finish client-branded document generation end-to-end: Excel/PPTX/slides/ZIP chrome, contract MDX studio, live BrandSetup letterhead draft, proposal preview matching export letterhead, and post-upload file preview.

**Architecture:** Extend existing `src/lib/letterhead.ts` helpers into all generators; reuse `MarkdownStudioEditor` and `DocumentFileViewer`; keep BrandProfile as the single client identity source for the workspace.

**Tech Stack:** Next.js 16, Prisma BrandProfile, ExcelJS, PPTXgenjs (existing), MDXEditor, Playwright PDF.

## Global Constraints

- Branch: `cursor/docs-generation-complete-ab64`
- No Etimad submission API, SSO/OIDC, live MyFatoorah, or AI bid pricing
- User-facing artifact titles/headers/authors must prefer BrandProfile + company letterhead over hardcoded `Arabclue` / `ArabClue` (platform disclaimer footer may still say “Generated by ArabClue”)
- Reuse `letterheadCompanyName`, brand colors, `resolveBrandFontStack`
- Bilingual AR/EN for new UI copy
- Commit after each task; run focused tests + `bunx tsc --noEmit`

---

### Task 1: Brand Excel / PPTX / slides / ZIP chrome

**Files:**
- Modify: `src/lib/generators.ts`
- Modify: `src/lib/letterhead.ts` (optional small helpers)
- Create/Modify: `src/lib/__tests__/generators-brand.test.ts`

**Interfaces:**
- Consumes: `BrandProfile | null`, `ProposalCompanyLetterhead | null`
- Produces: XLSX title/creator/header fill from brand; PPTX author/font/colors from brand; slides HTML company name; ZIP README uses company name

- [ ] **Step 1:** Add pure helper `brandArgb(hex: string): string` (strip `#`, prepend `FF`) in letterhead or generators; unit test.
- [ ] **Step 2:** In `generateComplianceMatrixXLSX` / BoQ sheets: set `wb.creator` and title cell to company/brand name; use `primaryColor` for header fill (stop `void brand`).
- [ ] **Step 3:** In PPTX + slides HTML generators: set author/presenter to company name; apply brand fontFace when mappable; use primary/accent colors.
- [ ] **Step 4:** ZIP README / package metadata: client company name first line.
- [ ] **Step 5:** Tests + tsc; commit: `feat(export): apply client brand to Excel PPTX slides ZIP`

---

### Task 2: Contract studio MDX + branded masthead

**Files:**
- Modify: `src/components/dashboard/contract-studio.tsx`
- Touch if needed: `contracts-panel.tsx` to pass brand or fetch `/api/brand`

**Interfaces:**
- Consumes: `MarkdownStudioEditor`, brand colors from `/api/brand`
- Produces: MDX edit mode; masthead shows company/brand not hardcoded ArabClue

- [ ] **Step 1:** Fetch brand in studio (or accept props); replace edit `Textarea` with `MarkdownStudioEditor` (`splitPreview` optional).
- [ ] **Step 2:** Replace hardcoded `ArabClue · أراب كلاو` masthead with brand tagline / company name.
- [ ] **Step 3:** tsc; commit: `feat(contracts): MDX studio with client branded masthead`

---

### Task 3: BrandSetup live letterhead from unsaved form

**Files:**
- Modify: `src/components/dashboard/brand-setup.tsx`

**Interfaces:**
- Produces: `LetterheadPreview` updates as user edits colors/logo/font/taglines before Save

- [ ] **Step 1:** Lift form state to parent (or pass `draft` from BrandForm via callback/controlled props) so preview tracks unsaved draft.
- [ ] **Step 2:** Preview uses draft; Save still PATCHes full draft including `fontFamily`.
- [ ] **Step 3:** tsc; commit: `fix(brand): live letterhead preview from unsaved draft`

---

### Task 4: Proposal preview matches export letterhead

**Files:**
- Modify: `src/components/dashboard/proposal-editor.tsx`
- Modify: `src/components/dashboard/markdown-studio-editor-inner.tsx` and/or `document-preview-frame.tsx`

**Interfaces:**
- Consumes: brand query already in proposal-editor; HTML download preview
- Produces: split/preview modes show letterhead bar (or default print mode uses DocumentPreviewFrame more prominently); paper chrome on DocumentPreviewFrame

- [ ] **Step 1:** Add optional letterhead strip above markdown preview using brand logo/colors/tagline (reuse CSS similar to `letterheadBarHtml` or fetch HTML preview iframe for split).
- [ ] **Step 2:** DocumentPreviewFrame: add subtle A4 paper shadow/chrome around iframe.
- [ ] **Step 3:** Prefer opening “print” preview with DocumentPreviewFrame when user clicks Preview mode (keep MDX for edit/split).
- [ ] **Step 4:** tsc; commit: `fix(studio): letterhead-aware proposal preview layout`

---

### Task 5: Open DocumentFileViewer after upload

**Files:**
- Modify: `src/components/dashboard/file-ingestion.tsx`
- Reuse: `document-file-viewer.tsx`

**Interfaces:**
- Consumes: upload API response with `storagePath`, `mimeType`, `originalName`
- Produces: auto-open viewer dialog on successful upload; click recent item opens viewer

- [ ] **Step 1:** Capture uploaded doc fields from POST `/api/documents` response.
- [ ] **Step 2:** Set `previewDoc` state and render `DocumentFileViewer`.
- [ ] **Step 3:** Recent list row click opens viewer when `storagePath` present.
- [ ] **Step 4:** tsc; commit: `feat(ingest): preview uploaded documents in file viewer`

---

## Out of scope

- Etimad portal submission API
- SSO / OIDC
- Live MyFatoorah charges
- New Prisma BrandProfile per government client (workspace BrandProfile remains the bidder letterhead)
